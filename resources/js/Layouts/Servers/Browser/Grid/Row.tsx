import React from 'react';

import {type ServerType} from '@/Components/Types';

const Row: React.FC<{
    server: ServerType
}> = ({ 
    server
}) => {
    return (
        <p>Placeholder.</p>
    );
}

export default Row;