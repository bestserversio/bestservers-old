import React from 'react';

import { type ServerStat } from '@/Components/Types';

const Stats: React.FC<{
    stats: ServerStat[]
}> = ({
    stats
}) => {
    // To Do: Generate stats graph via Google Charts or whatever library we go with.
    return (
        <>
        
        </>
    );
}

export default Stats;